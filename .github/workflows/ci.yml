name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: tarifaire_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Vérification syntaxique
        run: |
          for f in server/*.js server/services/*.js server/routes/*.js public/app.js public/sw.js public/docs.js tests/*.js; do
            node --check "$f"
          done
      - name: Tests unitaires et d'intégration
        run: npm test
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/tarifaire_test
