# Copier vers .env pour le développement local (Railway fournit ces variables en production)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tarifaire
PORT=3000
JWT_SECRET=changez-cette-valeur-en-production
# Compte administrateur créé au premier démarrage (base vide uniquement)
ADMIN_EMAIL=admin@demo.sn
ADMIN_PASSWORD=admin123
# PGSSL=1  # forcer TLS vers la base si nécessaire
